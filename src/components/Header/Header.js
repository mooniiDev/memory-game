// Components imports
import GameTitle from './GameTitle';
import ScoresSection from './ScoresSection';

function Header() {
  return (
    <header id="Header">
      <GameTitle />
      <ScoresSection />
    </header>
  );
}

export default Header;
